# bh-family: 核心家庭服务（端口 8788）
# OpenVINO 推理已拆分到 bh-openvino 独立服务
# Family 通过 K8s Service DNS (http://bh-openvino:8000) 调用 OpenVINO API
# 不再需要 Intel GPU 资源
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bh-family
  namespace: baihua
  labels:
    app: bh-family
    app.kubernetes.io/part-of: baihua
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bh-family
  template:
    metadata:
      labels:
        app: bh-family
    spec:
      containers:
        - name: bh-family
          image: bh-family:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8788
              name: http
          envFrom:
            - configMapRef:
                name: baihua-config
            - secretRef:
                name: baihua-secret
          env:
            - name: ASPNETCORE_URLS
              value: "http://0.0.0.0:8788"
            - name: BAIHUA_SKIP_MUTEX
              value: "true"
            # 移动端二维码入口地址：走 nginx 80 端口（无显式端口号），/mg/* 由 nginx 转发到本服务
            # 花记扫码后使用 http://<host>/（默认 80），不再出现 :8788 显式端口
            - name: Baihua__PublicBaseUrl
              value: "http://192.168.3.13"
            # 服务器互联（百花↔百花 互发消息）：入口地址自动探测（下行 API 注入节点 IP，广播/回发用）。
            # 如入口不在 80 或想用域名，可显式覆盖 BAIHUA_SERVER_PUBLIC_BASE_URL
            - name: BAIHUA_HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            # 管理 API 访问控制：放行 Pod 网段（WebUI 等 Pod 通过 Service 调用管理 API）
            - name: BAIHUA_ADMIN_ALLOWED_NETS
              value: "10.0.0.0/8"
            # OpenVINO 远程服务 URL（K8s Service DNS，bh-openvino = Intel OVMS）
            # 设为非 localhost 时，Family 跳过本地 Python 进程启动，直接调用远程 API
            - name: OPENVINO_LLM_URL
              value: "http://bh-openvino:8000"
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: logs
              mountPath: /app/logs
            - name: vaults
              mountPath: /vaults
            # OpenVINO 模型目录（只读挂载，用于扫描可用模型列表）
            - name: models
              mountPath: /opt/baihua/models
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8788
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8788
            initialDelaySeconds: 40
            periodSeconds: 30
            failureThreshold: 3
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 500m
              memory: 512Mi
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: baihua-data-pvc
        - name: logs
          persistentVolumeClaim:
            claimName: baihua-logs-pvc
        - name: vaults
          persistentVolumeClaim:
            claimName: baihua-vaults-pvc
        - name: models
          persistentVolumeClaim:
            claimName: baihua-models-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: bh-family
  namespace: baihua
  labels:
    app: bh-family
    app.kubernetes.io/part-of: baihua
spec:
  selector:
    app: bh-family
  # 固定 ClusterIP：宿主机 DSH/mcp-server 直接经该 IP 访问；固定后不随 Service 重建而改变，
  # 避免配置里的 familyUrl / BAIHUA_FAMILY_URL 失效。IP 必须在 k3s service CIDR（默认 10.43.0.0/16）内且空闲。
  clusterIP: 10.43.159.101
  ports:
    - name: http
      port: 8788
      targetPort: 8788
      protocol: TCP
